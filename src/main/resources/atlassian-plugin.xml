<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="Subtask"/>
    
    <!-- add our web resources -->
    <web-resource key="Subtask-resources" name="Subtask Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="Subtask.css" location="/css/Subtask.css"/>
        <resource type="download" name="Subtask.js" location="/js/Subtask.js"/>
        <resource type="download" name="images/" location="/images"/>

        <context>Subtask</context>
    </web-resource>

    <workflow-condition key="all-subtasks-done-condition"
    name="All Subtasks Done Condition"
    i18n-name-key="all-subtasks-done-condition.name"
    class="com.archimedis.jira.workflow.AllSubtasksDoneConditionFactory">
    
    <description key="all-subtasks-done-condition.description">
        Workflow condition that allows a transition only if all subtasks are in Done status.
    </description>

    <condition-class>
        com.archimedis.jira.workflow.AllSubtasksDoneCondition
    </condition-class>

    <resource type="velocity" name="view" location="templates/conditions/all-subtasks-done-condition.vm"/>
    <resource type="velocity" name="input-parameters" location="templates/conditions/all-subtasks-done-condition-input.vm"/>
    <resource type="velocity" name="edit-parameters" location="templates/conditions/all-subtasks-done-condition-input.vm"/>
</workflow-condition>
    
</atlassian-plugin>